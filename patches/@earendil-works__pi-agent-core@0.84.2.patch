diff --git a/dist/agent-loop.js b/dist/agent-loop.js
index 569149f9328920cda8c637ef4cdd0b11cac1a9eb..02f4814765228019892505e32fae669ca45c71bc 100644
--- a/dist/agent-loop.js
+++ b/dist/agent-loop.js
@@ -113,7 +113,7 @@ async function runLoop(initialContext, newMessages, initialConfig, signal, emit,
             // Check for tool calls
             const toolCalls = message.content.filter((c) => c.type === "toolCall");
             const toolResults = [];
-            hasMoreToolCalls = false;
+            hasMoreToolCalls = message.stopReason === "stop" && message.endTurn === false;
             if (toolCalls.length > 0) {
                 // A "length" stop means the output was cut off by the token limit, so
                 // every tool call in the message may carry truncated arguments. Fail
