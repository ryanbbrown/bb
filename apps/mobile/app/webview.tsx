// The WebView shell screen. Behind the client-local switch in
// Settings -> General until the shell replaces the native UI.
import { ProfileWebViewScreen } from "@/screens/webview";

export default function WebViewRoute() {
  return <ProfileWebViewScreen />;
}
