// Moved to @bb/client-core (shared with the native app); re-exported here so web imports keep resolving.
export {
  NO_MACHINE_GROUP_KEY,
  buildMachineThreadGroups,
} from "@bb/client-core";
export type { MachineThreadGroup } from "@bb/client-core";
