/**
 * Monaco's base editor worker. It backs the editor's own background work —
 * diff computation, link detection, word-based suggestions — not language
 * services, which this bundle omits.
 */
import "monaco-editor/editor/editor.worker.start.js";
